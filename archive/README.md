# Archive — stale single-file snapshots

These are OLD single-file bundles kept only for reference. They are NOT served.
The authoritative app is the multi-file source in the parent folder:
index.html + data.jsx + entities.jsx + icons.jsx + store.jsx + shell.jsx + pages.jsx + app.jsx.
If a single-file distributable is ever needed, regenerate it with build.py from those sources.
