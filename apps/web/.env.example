# Base URL of the APH RSS proxy Worker.
# Local dev (wrangler dev): http://127.0.0.1:8787
# Production: https://aph-proxy.<your-subdomain>.workers.dev
VITE_API_BASE=https://aph-proxy.jvega019.workers.dev
# For local Worker dev instead, set:
# VITE_API_BASE=http://127.0.0.1:8787
